# V2 Startup Incubation Consulting Portal - Environment Configuration
# Duplicate this file as .env or .env.local for local development

# The target REST API endpoint (such as a Make.com webhook) where lead data payloads will be POSTed.
VITE_WEBHOOK_URL=your_url
